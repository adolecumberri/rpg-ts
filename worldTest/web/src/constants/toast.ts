// Fixed values for the web UI (every fixed value gets its own home).
export const TOAST_MS = {
    // Regular feedback toasts: saved, purchased, leveled up...
    default: 2400,
    // Tap-help explanations on stat chips: long enough to read.
    help: 5000,
} as const;
